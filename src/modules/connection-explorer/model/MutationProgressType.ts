export enum MutationProgressType {
    Duplication = 'duplication',
    Rename = 'rename',
    Replace = 'replace',
    Activate = 'activate',
    Deactivate = 'deactivate',
    Mutable = 'mutable',
    Immutable = 'immutable',
    Alive = 'alive'
}
