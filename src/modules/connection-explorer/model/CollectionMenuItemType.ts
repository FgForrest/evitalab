/**
 * List of actions that can be performed on a collection.
 */
export enum CollectionMenuItemType {
    Entities = 'entities',
    Schema = 'schema',

    ModifySubheader = 'modifySubheader',
    DeleteCollection = 'deleteCollection',
    RenameCollection = 'renameCollection'
}
